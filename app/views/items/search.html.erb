<div class="top-search-image">
  <div class="top-header">

    <%= render "shared/menu" %>
  </div>
  <div class="accordion-category-search">
    <br/><h3>アイテムを探す🎁</h3>


    <%# 検索フォーム %>
    <%= form_with(url: search_items_path, local:true, method: :get, class: "search-form") do |form| %>
      <%= form.text_field :keyword, placeholder: "（アイテム名）", class: "search-placeholder" %>
      <%= form.submit "さがす", class: "search-btn" %>
    <% end %>

  
    <% @items.reverse_each do |item| %>
      <div class="item-list">
        <%= render partial: "shared/item", locals: { item: item } %>
      </div>
    <% end %>
  </div>
</div>