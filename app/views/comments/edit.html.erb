<div class="contents row">
  <div class="container">
    <h3>編集する</h3>
    <%#= form_with(model: @item.comment.id, local: true) do |form| %>
    <%#= form_with(url: item_comment_path, local:true, method: :GET, class: "edit-form") do |form| %>
      <%#= form.text_field :comment, placeholder: "コメント" %>
      <%# form.submit  "編集OK" %>
      
    <%# end %>
  </div>
</div>