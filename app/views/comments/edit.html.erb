<div class="contents row">
  <div class="container">
    <h3>編集する</h3>
    <%= form_with(model: @comment, local: true) do |form| %>
      <%= form.text_field :comment, placeholder: "コメント" %>
      <%= form.submit "編集OK" %>
    <% end %>
  </div>
</div>